## Full Depth PTree Generator
```
node index.js
```

This module generates dynamic PTree from script in `INPUT_PATH` with each properties' values(function body, etc.) into `ALL_OUTPUT_PATH` and extracts function name and its body in `FUNC_OUTPUT_PATH`
